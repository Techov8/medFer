<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/strip_ad_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="10dp"
    android:layout_marginEnd="10dp"
    android:layout_marginBottom="5dp"
    android:background="#ffffff">

    <ImageView
        android:id="@+id/strip_ad_image"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="#ffffff"
        android:scaleType="fitXY"
        android:src="@drawable/placeholder"
        app:srcCompat="@drawable/placeholder" />

    <Button
        android:id="@+id/health_card_btn"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_below="@id/strip_ad_image"
        android:background="@color/colorPrimaryDark"
        android:elevation="2dp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:text="Upgrade to inSurePlus"
        android:textAllCaps="false"
        android:textColor="@color/lightWhite"
        android:textSize="16sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/imageView4"
        android:layout_width="30dp"
        android:layout_height="25dp"
        android:layout_below="@id/strip_ad_image"
        android:layout_alignEnd="@id/health_card_btn"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="15dp"
        android:elevation="3dp"
        app:srcCompat="@drawable/right_arrow" />
</RelativeLayout>